apiVersion: v1
kind: ConfigMap
metadata:
  name: docker-daemon-config
data:
  daemon.json: |
    {
      "insecure-registries": ["docker-registry-service.docker-registry.svc.cluster.local"],
      "experimental": true,
      "features": {
        "buildkit": true
      },
      "builder": {
        "network": "host"
      }
    }
