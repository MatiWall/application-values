apiVersion: v1
kind: ConfigMap
metadata:
  name: tempo-config
  namespace: monitoring
data:
  tempo-config.yaml: |-
    storage:
      trace:
        backend: local
        local:
          directory: /etc/tempo/data
